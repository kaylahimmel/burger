// if burger is devoured, on the index.handlebars page, move it from "available" list to "eaten" div


// update "devoured" status in the MySQL table
// I think this is already happening in burgers_controllers.js, lines 38-54